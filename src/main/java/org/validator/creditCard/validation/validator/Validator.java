package org.validator.creditCard.validation.validator;

// An interface that defines common behavior for validators
public interface Validator {
    // An abstract method that validates a given number
    boolean isValid();
}
